[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmd = "npm install -g pnpm && pnpm install --frozen-lockfile"

[phases.build]
cmd = "pnpm --filter backend build"

[start]
cmd = "node apps/backend/dist/server.js"
